// The shortest js program is an empty file
// window is a globla object it is create along with global execution context
// js just not run on browsers but in servers and other places so it runs on some engines
// these engines have the responsibilities ot craete these global objects
// at global level this==window